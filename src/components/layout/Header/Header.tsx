import React from 'react';

export default function Header() {
  return (
    <header>
      <div>
        <button
          //   onClick={openModal}
          type="button"
          className="text-white"
        >
          HEADER BTN
        </button>
      </div>
    </header>
  );
}
